# 백엔드 API 베이스 URL. 예: https://api.example.com/v1
VITE_API_BASE_URL=http://localhost:8080/api

# true 이면 src/mocks 의 목데이터로 동작한다. 백엔드를 붙일 때 false 로 바꾼다.
VITE_USE_MOCK=true

# 공유 링크에 표시할 도메인. 비워두면 현재 접속한 origin 을 사용한다.
VITE_SHARE_LINK_ORIGIN=
