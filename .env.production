# 백엔드가 아직 없어 배포본도 목데이터로 동작한다.
#
# 백엔드가 준비되면 이 파일을 지우거나, 호스팅 대시보드(Vercel > Settings >
# Environment Variables)에 값을 넣으면 된다. Vite 는 실행 시점에 이미 존재하는
# 환경변수를 .env 파일보다 우선하므로 대시보드 값이 항상 이깁니다.
VITE_USE_MOCK=true
